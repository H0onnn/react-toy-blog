import { css } from "styled-components";

export const text_hidden = css`
  text-overflow: ellipsis;
  white-space: nowrap;
  overflow: hidden;
`;

export const full_img_cover = css`
  width: 100%;
  height: 100%;
  object-fit: cover;
`;
